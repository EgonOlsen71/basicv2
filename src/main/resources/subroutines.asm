START		RTS

END			RTS

ARRAYACCESS	RTS

ARRAYSTORE	RTS

INITFOR		RTS

NEXT		RTS

RETURN		RTS

GOSUB		RTS

COMPACT		RTS

GETSTR		RTS

SEQ			RTS

READNUMBER	RTS

PUSHREAL	LDX #<FPSTACKP
			LDY #>FPSTACKP
			JSR $BBD7
			LDA FPSTACKP
			CLC
			ADC #5
			STA FPSTACKP
			LDA FPSTACKP+1
			ADC #0
			STA FPSTACKP+1
			RTS

PUSHINT		LDX FPSTACKP
			STX TMP2_ZP
			LDX FPSTACKP+1
			STX TMP2_ZP+1
			LDA TMP_ZP
			LDY #0
			STA (TMP2_ZP),Y
			LDA TMP_ZP+1
			INY
			STA (TMP2_ZP),Y
			LDA TMP2_ZP
			CLC
			ADC #2
			STA FPSTACKP
			LDA TMP2_ZP+1
			ADC #0
			STA FPSTACKP+1
			RTS
			
POPREAL		LDA FPSTACKP
			SEC
			SBC #5
			STA FPSTACKP
			LDA FPSTACKP+1
			SBC #0
			STA FPSTACKP+1
			LDA #<FPSTACKP
			LDY #>FPSTACKP
			JSR $BBA2
			RTS
			
POPINT		LDA FPSTACKP
			SEC
			SBC #2
			STA FPSTACKP
			LDA FPSTACKP+1
			SBC #0
			STA FPSTACKP+1
			LDX FPSTACKP
			STX TMP2_ZP
			LDX FPSTACKP+1
			STX TMP2_ZP+1
			LDY #0
			LDA (TMP2_ZP),Y
			STA TMP_ZP
			INY
			LDA (TMP2_ZP),Y
			STA TMP_ZP+1
			RTS
			
			
